#include <bits/stdc++.h>
typedef long long Long;
#define para(i,n) for(Long i=0;i<(Long)n;i++)
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    Long n;
    cin>>n;
    
    for(Long t=0;t<n;t++){
        Long m;
        cin>>m;
        for(Long r=0;r<m;r++){
            Long aux;
            cin>>aux;
        }
        Long resp=(m*(m-1))/2;
        cout<<resp<<endl;
    }
}