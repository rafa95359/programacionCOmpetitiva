#include <bits/stdc++.h>
using namespace std;
typedef long long Long;
#define para(i,n) for(Long i=0;i<(Long)n;i++)
Long arr[1001];
int main(){
    float n;
    cin>>n;       
    cout<<pow(n,1)<<endl;
    Long aux=pow(n,1);
    cout<<aux<<endl;
    return 0;
}
